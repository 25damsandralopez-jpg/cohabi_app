# Cohabi v6 - Solicitudes tenant

1. Ejecuta `supabase/migrations/20260824_tenant_applications_flow.sql` en Supabase SQL Editor.
2. Copia la carpeta `lib/` sobre tu proyecto.
3. Ejecuta `flutter clean`, `flutter pub get`, `flutter run`.

Incluye: detalle de piso, envío de interés, confirmación, solicitudes y propuesta/confirmación de visitas.
